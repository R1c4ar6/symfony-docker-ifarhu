{%extends 'base.html.twig' %}

{% block title %}Inicio
{% endblock %}

{% block body %}

	{% if students is not empty %}
		<table class="table">
			<thead>
				<tr>
					<th scope="col">Nombre</th>
					<th scope="col">Apellido</th>
					<th scope="col">Cédula</th>
					<th scope="col">Docs</th>
				</tr>
			</thead>
			<tbody>
				{% for student in students %}
					<tr>
						<td>{{ student.firstName }}</td>
						<td>{{ student.lastName }}</td>
						<td>{{ student.identificationNumber }}</td>
						{# <td><a href="{{ path('app_document_show', {'id': student.id}) }}" class="btn btn-success">Ver</a></td> #}
					</tr>
				{% endfor %}
			</tbody>
		</table>
	{% else %}
		<p class="text-center text-danger mt-5 fw-bold h1">No hay estudiantes registrados</p>
	{% endif %}

{% endblock %}
