{% extends 'base.html.twig' %}

{% block title %}Nuevo Estudiante
{% endblock %}

{% block body %}
	{{ parent() }}
	<div class="container mt-5">
	  <div class="row justify-content-center">
		<div class="col-md-8">
		  <div class="card">
			<div class="card-body">
			  <h1 class="text-center mb-4">Nuevo estudiante</h1>
			  {{ include('student/_form.html.twig') }}
			  <div class="text-center mt-4">
				<a href="{{ path('app_student_index') }}" class="btn btn-secondary">Volver a la lista</a>
			  </div>
			</div>
		  </div>
		</div>
	  </div>
	</div>


{% endblock %}
