{% extends 'base.html.twig' %}

{% block title %}Nuevo Estudiante
{% endblock %}

{% block body %}
	{{ parent() }}
	<div class="container">
		<div class="row align-content-center">
			<h1 class="text-center">Nuevo estudiante</h1>

			{{ include('student/_form.html.twig') }}

			<a href="{{ path('app_student_index') }}">Volver a la lista</a>
		</div>
	</div>


{% endblock %}
