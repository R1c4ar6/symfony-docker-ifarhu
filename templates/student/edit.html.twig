{% extends 'base.html.twig' %}

{% block title %}Edit Student
{% endblock %}

{% block body %}
	{{ parent() }}
	<div class="container">
		<div class="row align-content-center">
			<h1 class="text-center">Editar estudiante</h1>

			{{ include('student/_form.html.twig', {'button_label': 'Actualizar'}) }}

			<a href="{{ path('app_student_index') }}" class="mt-3">Volver a la lista</a>

			{{ include('student/_delete_form.html.twig') }}
		</div>
	</div>


{% endblock %}
