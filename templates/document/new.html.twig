{% extends 'base.html.twig' %}

{% block title %}Nuevo Documento
{% endblock %}

{% block body %}
	{{ parent() }}
	<div class="container">
		<div class="row align-content-center">
			<h1 class="text-center">Nuevo documento</h1>

			{{ include('document/_form.html.twig') }}

			<a class="mt-3" href="{{ path('app_student_documents_show', {'id': student_id}) }}">Volver a los documentos</a>
		</div>
	</div>
{% endblock %}
